# Examples

This directory contains importable project examples for the current two-fragment FusionPCR Studio workflow.

Available examples:

- `exact-fusion/project.json`
- `protein-fusion/project.json`
- `insertion/project.json`
- `mutation/project.json`

Each example uses the current `0.2.0` project schema and can be loaded through the application's project import flow.
